% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S7-classes.R, R/s7-foundation.R
\name{UVG-CategoryRepresentation-class}
\alias{UVG-CategoryRepresentation-class}
\alias{UVG_CategoryRepresentation}
\title{UVG Category Representation}
\usage{
UVG_CategoryRepresentation(
  category_labels = character(0),
  cue_labels = character(0),
  category_likelihood = function() NULL,
  metadata = list(),
  mu = integer(0),
  sigma2 = integer(0)
)

UVG_CategoryRepresentation(
  category_labels = character(0),
  cue_labels = character(0),
  category_likelihood = function() NULL,
  metadata = list(),
  mu = integer(0),
  sigma2 = integer(0)
)
}
\description{
Category-level representation for a univariate Gaussian (\code{UVG}) family.

Category-level representation for a univariate Gaussian (\code{UVG}) family.
}
\details{
Assumed parameterization:
\itemize{
\item \code{mu}: scalar mean.
\item \code{sigma2}: scalar variance, constrained to be positive.
}

Structural assumptions:
\itemize{
\item exactly one cue dimension (\code{length(cue_labels) == 1}).
}

Category likelihood:
for observation \eqn{x},

\deqn{p(x\mid c) = \mathcal{N}(x;\mu_c,\sigma_c^2).}

With \code{log = TRUE}, the implementation returns
\eqn{\log p(x\mid c)}.

Assumed parameterization:
\itemize{
\item \code{mu}: scalar mean.
\item \code{sigma2}: scalar variance, constrained to be positive.
}

Structural assumptions:
\itemize{
\item exactly one cue dimension (\code{length(cue_labels) == 1}).
}

Category likelihood:
for observation \eqn{x},

\deqn{p(x\mid c) = \mathcal{N}(x;\mu_c,\sigma_c^2).}

With \code{log = TRUE}, the implementation returns
\eqn{\log p(x\mid c)}.
}
\seealso{
\link{UVG_IdealObserver}, \link{NIX_CategoryRepresentation}
}
\keyword{internal}
