% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S7-family-coercion.R
\name{as_family_coercion}
\alias{as_family_coercion}
\alias{as_uvg_category_representation}
\alias{as_nix_category_representation}
\alias{as_muvg_category_representation}
\alias{as_mnix_category_representation}
\alias{as_mvg_category_representation}
\alias{as_niw_category_representation}
\alias{as_exemplar_category_representation}
\alias{as_uvg_category_representation_template}
\alias{as_nix_category_representation_template}
\alias{as_muvg_category_representation_template}
\alias{as_mnix_category_representation_template}
\alias{as_mvg_category_representation_template}
\alias{as_niw_category_representation_template}
\alias{as_exemplar_category_representation_template}
\alias{as_uvg_ideal_observer}
\alias{as_nix_ideal_adaptor}
\alias{as_muvg_ideal_observer}
\alias{as_mnix_ideal_adaptor}
\alias{as_mvg_ideal_observer}
\alias{as_niw_ideal_adaptor}
\alias{as_exemplar_model}
\title{Coerce category representations, templates, and cognitive models between families}
\usage{
as_uvg_category_representation(x)

as_nix_category_representation(x, kappa, nu)

as_muvg_category_representation(x, component_weights = NULL)

as_mnix_category_representation(x, kappa, nu, component_weights = NULL)

as_mvg_category_representation(x)

as_niw_category_representation(x, kappa, nu)

as_exemplar_category_representation(x, n)

as_uvg_category_representation_template(x)

as_nix_category_representation_template(x, kappa, nu)

as_muvg_category_representation_template(x, component_weights = NULL)

as_mnix_category_representation_template(
  x,
  kappa,
  nu,
  component_weights = NULL
)

as_mvg_category_representation_template(x)

as_niw_category_representation_template(x, kappa, nu)

as_exemplar_category_representation_template(x, n)

as_uvg_ideal_observer(x)

as_nix_ideal_adaptor(x, kappa, nu)

as_muvg_ideal_observer(x, component_weights = NULL)

as_mnix_ideal_adaptor(x, kappa, nu, component_weights = NULL)

as_mvg_ideal_observer(x)

as_niw_ideal_adaptor(x, kappa, nu)

as_exemplar_model(x, n)
}
\arguments{
\item{x}{An MVBU_CategoryRepresentation, MVBU_CategoryRepresentationTemplate, or MVBU_CognitiveModel object.}

\item{kappa}{Strength of belief (pseudocount) about the category mean.}

\item{nu}{Strength of belief (pseudocount) about the category covariance/variance.}

\item{component_weights}{Optional per-component/per-cue weights for MUVG/MNIX targets. (default: preserved
from \code{x} if available, otherwise the family constructor's default.)}

\item{n}{Number of exemplars to sample when coercing into the EXEMPLAR family.}
}
\value{
An object of the same kind as \code{x} (representation, template, or model), coerced to the target family.
}
\description{
These functions coerce between the point-estimate families (UVG, MUVG, MVG), the corresponding
uncertainty families (NIX, MNIX, NIW), and the non-parametric Exemplar family. Supported coercions
are: same-family (returned unchanged), matched pairs (UVG<->NIX, MUVG<->MNIX, MVG<->NIW), and any
family <-> EXEMPLAR. Coercions into NIX/MNIX/NIW require \code{kappa} and \code{nu}; coercions into EXEMPLAR
require \code{n}, the number of exemplars to sample.
}
