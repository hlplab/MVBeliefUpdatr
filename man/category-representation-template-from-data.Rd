% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S7-make-objects.R
\name{new_uvg_category_representation_template_from_data}
\alias{new_uvg_category_representation_template_from_data}
\alias{new_nix_category_representation_template_from_data}
\alias{new_muvg_category_representation_template_from_data}
\alias{new_mnix_category_representation_template_from_data}
\alias{new_mvg_category_representation_template_from_data}
\alias{new_niw_category_representation_template_from_data}
\alias{new_exemplar_category_representation_template_from_data}
\alias{new_category_representation_template_from_data}
\title{Construct a UVG category-representation template from data.}
\usage{
new_uvg_category_representation_template_from_data(
  data,
  category = "category",
  cues,
  verbose = FALSE
)

new_nix_category_representation_template_from_data(
  data,
  category = "category",
  cues,
  kappa = nu,
  nu = 3,
  verbose = FALSE
)

new_muvg_category_representation_template_from_data(
  data,
  category = "category",
  cues,
  verbose = FALSE
)

new_mnix_category_representation_template_from_data(
  data,
  category = "category",
  cues,
  kappa = nu,
  nu = 3,
  verbose = FALSE
)

new_mvg_category_representation_template_from_data(
  data,
  category = "category",
  cues,
  verbose = FALSE
)

new_niw_category_representation_template_from_data(
  data,
  category = "category",
  cues,
  kappa = nu,
  nu = length(cues) + 2,
  verbose = FALSE
)

new_exemplar_category_representation_template_from_data(
  data,
  category = "category",
  cues,
  verbose = FALSE
)

new_category_representation_template_from_data(
  data,
  type,
  category = "category",
  cues,
  ...
)
}
\arguments{
\item{data}{A tibble or data.frame with one row per observation.}

\item{category}{Name of the column in \code{data} that contains the category information.}

\item{cues}{Names of the columns in \code{data} that contain the cue information.}

\item{verbose}{Should a message about the constructed template be printed? (default: \code{FALSE})}

\item{kappa}{Strength of belief (pseudocount) about the category mean. (default: same as \code{nu})}

\item{nu}{Strength of belief (pseudocount) about the category covariance matrix. (default: number of cues + 2)}

\item{type}{Category-representation template type: \code{"UVG"}, \code{"NIX"}, \code{"MUVG"}, \code{"MNIX"}, \code{"MVG"}, \code{"NIW"}, or \code{"EXEMPLAR"}.}

\item{...}{Arguments forwarded to the selected constructor, such as \code{kappa} and \code{nu}.}
}
\value{
An \code{MVBU_CategoryRepresentationTemplate} of UVG representations.

An \code{MVBU_CategoryRepresentationTemplate} of NIX representations.

An \code{MVBU_CategoryRepresentationTemplate} of MUVG representations.

An \code{MVBU_CategoryRepresentationTemplate} of MNIX representations.

An \code{MVBU_CategoryRepresentationTemplate} of MVG category representations.

An \code{MVBU_CategoryRepresentationTemplate} of NIW category representations.

An \code{MVBU_CategoryRepresentationTemplate} of Exemplar category representations.

An \code{MVBU_CategoryRepresentationTemplate} object.
}
\description{
Estimates the per-category mean and covariance matrix of the cues, and wraps them into an
\code{MVBU_CategoryRepresentationTemplate} of MVG category representations.

Estimates the per-category mean and covariance matrix of the cues (as in
\code{\link{new_mvg_category_representation_template_from_data}}), treats them as the expected category mean
and covariance, and derives the NIW \code{m}/\code{S} parameters from the user-provided \code{kappa}/\code{nu}.
}
