% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S7-core-classes.R
\name{MVBU-core-classes}
\alias{MVBU-core-classes}
\alias{MVBU_Object}
\alias{MVBU_CategoryRepresentation}
\alias{MVBU_CategoryRepresentationTemplate}
\alias{MVBU_CognitiveModel}
\title{MVBU Core S7 Class Architecture}
\usage{
MVBU_Object()

MVBU_CategoryRepresentation(
  category_likelihood_function = function() NULL,
  metadata = list()
)

MVBU_CategoryRepresentationTemplate(
  representations = list(),
  metadata = list()
)

MVBU_CognitiveModel(
  category_template = (structure(function (representations = list(), metadata = list()) 

    new_object(MVBU_Object(), representations = representations, metadata = metadata),
    name = "MVBU_CategoryRepresentationTemplate", parent = structure(function () 
 {
    
    S7::new_object(S7::S7_object())
 }, name = "MVBU_Object", parent = structure(function
    () 
 {
     .Call(S7_object_)
 }, name = "S7_object", properties = list(), abstract =
    FALSE, constructor = function () 
 {
     .Call(S7_object_)
 }, validator = function
    (self) 
 {
     if (!is_S7_type(self)) {
         "Underlying data is corrupt"
    
    }
 }, class = c("S7_class", "S7_object")), properties = list(), abstract = FALSE,
    constructor = function () 
 {
     S7::new_object(S7::S7_object())
 }, class =
    c("S7_class", "S7_object")), properties = list(representations = structure(list(name
    = "representations", class = structure(list(class = "list", constructor_name =
    "list", constructor = function (.data = list()) 
 .data, validator = function
    (object) 
 {
     if (base_class(object) != name) {
        
    sprintf("Underlying data must be <\%s> not <\%s>", name, base_class(object))
     }

    }), class = "S7_base_class"), getter = NULL, setter = NULL, validator = NULL, default
    = NULL), class = "S7_property"), metadata = structure(list(name = "metadata", class =
    structure(list(class = "list", constructor_name = "list", constructor = function
    (.data = list()) 
 .data, validator = function (object) 
 {
     if
    (base_class(object) != name) {
        
    sprintf("Underlying data must be <\%s> not <\%s>", name, base_class(object))
     }

    }), class = "S7_base_class"), getter = NULL, setter = NULL, validator = NULL, default
    = NULL), class = "S7_property")), abstract = FALSE, constructor = function
    (representations = list(), metadata = list()) 
 new_object(MVBU_Object(),
    representations = representations, metadata = metadata), validator = function (self)
    
 {
     if (length(self@representations) < 1) {
        
    return(paste0("representations must contain at least one ",
    "category representation object."))
     }
     if (!all(vapply(self@representations,
    function(r) S7::S7_inherits(r, MVBU_CategoryRepresentation), logical(1)))) {
        
    return(paste0("all representations entries must inherit from ",
    "MVBU_CategoryRepresentation."))
     }
     repr_names <-
    names(self@representations)
     if (!is.null(repr_names)) {
         if
    (length(repr_names) != length(self@representations) || any(repr_names == "") ||
    anyDuplicated(repr_names) > 0) {
            
    return("if representations are named, names must be non-empty and unique.")
        
    }
     }
     NULL
 }, class = c("S7_class", "S7_object")))(),
  category_posterior_functions = list(),
  decision_rule = character(0),
  category_prior = integer(0),
  lapse_behavior = list(),
  noise_behavior = list(),
  metadata = list()
)
}
\description{
The MVBeliefUpdatr S7 organizes model objects around an explicit
compositional structure. These classes define the canonical S7 API; any
older list/data-frame-style inputs are supported only through transitional
compatibility methods and should be phased out over time.
}
\details{
\itemize{
\item \link{MVBU_CategoryRepresentation} defines the structure for one category-level
representational object. This could be a uni- or multivariate Gaussian,
a mixture of Gaussians, or an exemplar-based representation.
\item \link{MVBU_CategoryRepresentationTemplate} collects one or more
category-representation objects into a validated set used by a model.
This corresponds to the notion of "templates" in e.g.,
\insertCite{nearey-assmann2007}{MVBeliefUpdatr}.
\item \link{MVBU_CognitiveModel} combines category-template structure with model-level
decision behavior (\code{decision_rule}, \code{category_prior},
\code{lapse_rate}, \code{lapse_bias}).
}

Family-level semantics are layered on top of these base classes.
Ideal observer/adaptor pairings follow:
\itemize{
\item \link{UVG_IdealObserver} \code{UVG} <-> \link{NIX_IdealAdaptor} \code{NIX}
\item \link{MUVG_IdealObserver} \code{MUVG} <-> \link{MNIX_IdealAdaptor} \code{MNIX}
\item \link{MVG_IdealObserver} \code{MVG} <-> \link{NIW_IdealAdaptor} \code{NIW}
}

while \link{Exemplar_Model} \code{EXEMPLAR} is treated as a standalone family.
}
\section{MVBU_Object}{

Root S7 base class for MVBeliefUpdatr objects.
}

\section{MVBU_CategoryRepresentation}{

Abstract category-level representational class.

Expected properties:
\itemize{
\item \code{category_labels}: one or more category labels associated with the object.
\item \code{cue_labels}: one or more cue-dimension labels.
\item \code{category_likelihood_function}: family-specific likelihood function
placeholder.
\item \code{metadata}: optional auxiliary metadata list.
}
}

\section{MVBU_CategoryRepresentationTemplate}{

Container class for per-category representation objects.

Expected properties:
\itemize{
\item \code{representations}: list of objects inheriting from
\code{MVBU_CategoryRepresentation}.
\item \code{metadata}: optional template-level metadata.
}

Validators enforce at least one representation and (if named) unique,
non-empty representation names.
}

\section{MVBU_CognitiveModel}{

Abstract model class binding a category-representation template to
model-level decision and uncertainty parameters.

Expected properties:
\itemize{
\item \code{category_template}: \code{MVBU_CategoryRepresentationTemplate}.
\item \code{category_posterior_functions}: named list of treatment-specific
posterior functions.
\item \code{decision_rule}: scalar character rule label.
\item \code{category_prior}: numeric probability vector over represented categories.
\item \code{lapse_behavior}: list with \code{lapse_rate}, \code{lapse_bias}, and
\code{lapse_treatment}.
\item \code{noise_behavior}: list with \code{Sigma_noise} and \code{noise_treatment}.
\code{Sigma_noise} is stored as a square matrix and may be supplied as a vector
(which is converted to a diagonal matrix) or as a matrix.
\item \code{metadata}: optional model metadata.
}
}

\references{
\insertAllCited{}

\insertRef{xie2023}{MVBeliefUpdatr}

Xie, X., Jaeger, T. F., & Kurumada, C. (2021). What we do (not) know about
the mechanisms underlying adaptive speech perception: A computational review.
}
\keyword{internal}
