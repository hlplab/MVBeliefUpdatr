% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S7-make-objects.R
\name{new_uvg_ideal_observer_from_data}
\alias{new_uvg_ideal_observer_from_data}
\alias{new_nix_ideal_adaptor_from_data}
\alias{new_muvg_ideal_observer_from_data}
\alias{new_mnix_ideal_adaptor_from_data}
\alias{new_mvg_ideal_observer_from_data}
\alias{new_niw_ideal_adaptor_from_data}
\alias{new_exemplar_model_from_data}
\alias{new_model_from_data}
\title{Construct a UVG ideal observer from data.}
\usage{
new_uvg_ideal_observer_from_data(
  data,
  category = "category",
  cues,
  decision_rule = "sampling",
  category_prior = NULL,
  lapse_rate = 0,
  lapse_bias = NULL,
  Sigma_noise = NULL,
  noise_treatment = "no_noise",
  lapse_treatment = "no_lapses",
  verbose = FALSE
)

new_nix_ideal_adaptor_from_data(
  data,
  category = "category",
  cues,
  kappa = nu,
  nu = 3,
  decision_rule = "sampling",
  category_prior = NULL,
  lapse_rate = 0,
  lapse_bias = NULL,
  Sigma_noise = NULL,
  noise_treatment = "no_noise",
  lapse_treatment = "no_lapses",
  verbose = FALSE
)

new_muvg_ideal_observer_from_data(
  data,
  category = "category",
  cues,
  decision_rule = "sampling",
  category_prior = NULL,
  lapse_rate = 0,
  lapse_bias = NULL,
  Sigma_noise = NULL,
  noise_treatment = "no_noise",
  lapse_treatment = "no_lapses",
  verbose = FALSE
)

new_mnix_ideal_adaptor_from_data(
  data,
  category = "category",
  cues,
  kappa = nu,
  nu = 3,
  decision_rule = "sampling",
  category_prior = NULL,
  lapse_rate = 0,
  lapse_bias = NULL,
  Sigma_noise = NULL,
  noise_treatment = "no_noise",
  lapse_treatment = "no_lapses",
  verbose = FALSE
)

new_mvg_ideal_observer_from_data(
  data,
  category = "category",
  cues,
  decision_rule = "sampling",
  category_prior = NULL,
  lapse_rate = 0,
  lapse_bias = NULL,
  Sigma_noise = NULL,
  noise_treatment = "no_noise",
  lapse_treatment = "no_lapses",
  verbose = FALSE
)

new_niw_ideal_adaptor_from_data(
  data,
  category = "category",
  cues,
  kappa = nu,
  nu = length(cues) + 2,
  decision_rule = "sampling",
  category_prior = NULL,
  lapse_rate = 0,
  lapse_bias = NULL,
  Sigma_noise = NULL,
  noise_treatment = "no_noise",
  lapse_treatment = "no_lapses",
  verbose = FALSE
)

new_exemplar_model_from_data(
  data,
  category = "category",
  cues,
  decision_rule = "sampling",
  category_prior = NULL,
  lapse_rate = 0,
  lapse_bias = NULL,
  Sigma_noise = NULL,
  noise_treatment = "no_noise",
  lapse_treatment = "no_lapses",
  verbose = FALSE
)

new_model_from_data(data, type, category = "category", cues, ...)
}
\arguments{
\item{data}{A tibble or data.frame with one row per observation.}

\item{category}{Name of the category column in \code{data}.}

\item{cues}{Names of the cue columns in \code{data}.}

\item{decision_rule}{Decision rule of the constructed model. (default: "sampling")}

\item{category_prior}{Optional category-prior vector. (default: uniform over categories)}

\item{lapse_rate}{Optional lapse rate. (default: 0)}

\item{lapse_bias}{Optional lapse-bias vector. (default: same as \code{category_prior})}

\item{Sigma_noise}{Optional perceptual-noise covariance matrix. (default: \code{NULL}, i.e., no noise)}

\item{noise_treatment}{How perceptual noise is treated during categorization. (default: "no_noise")}

\item{lapse_treatment}{How attentional lapses are treated during categorization. (default: "no_lapses")}

\item{verbose}{Should a construction message be printed? (default: \code{FALSE})}

\item{kappa}{Strength of belief about component means. (default: same as \code{nu})}

\item{nu}{Strength of belief about component variances. (default: \code{3})}

\item{type}{Model type: \code{"UVG"}, \code{"NIX"}, \code{"MUVG"}, \code{"MNIX"}, \code{"MVG"}, \code{"NIW"}, or \code{"EXEMPLAR"}.}

\item{...}{Arguments forwarded to the selected model constructor.}
}
\value{
A \code{UVG_IdealObserver} object.

A \code{NIX_IdealAdaptor} object.

A \code{MUVG_IdealObserver} object.

A \code{MNIX_IdealAdaptor} object.

An \code{MVG_IdealObserver} object.

A \code{NIW_IdealAdaptor} object.

An \code{Exemplar_Model} object.

An S7 cognitive-model object.
}
\description{
Construct a UVG ideal observer from data.

Construct a NIX ideal adaptor from data.

Construct a MUVG ideal observer from data.

Construct a MNIX ideal adaptor from data.

Construct an MVG ideal observer from data.

Construct a NIW ideal adaptor from data.

Construct an Exemplar model from data.

Construct a cognitive model from data by type.
}
