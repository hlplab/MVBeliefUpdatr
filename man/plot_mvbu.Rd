% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S7-generics.R, R/S7-plot-methods.R
\name{plot_categories}
\alias{plot_categories}
\alias{plot_categorization_function}
\alias{plot_parameters}
\alias{plot_parameter_correlations}
\alias{plot_parameters_pairwise}
\alias{plot_cues}
\alias{plot_diagnostics}
\alias{plot_categories,MVBU_CategoryRepresentation-method}
\alias{plot_categories,MVBU_CategoryRepresentationTemplate-method}
\alias{plot_categories,MVBU_CognitiveModel-method}
\alias{plot_categories,MVBU_Stanfit-method}
\alias{plot_categorization_function,MVBU_CognitiveModel-method}
\alias{plot_categorization_function,MVBU_Stanfit-method}
\alias{plot_parameters,MVBU_CognitiveModel-method}
\alias{plot_parameters,MVBU_Stanfit-method}
\alias{plot_parameter_correlations,MVBU_Stanfit-method}
\alias{plot_parameters_pairwise,MVBU_Stanfit-method}
\alias{plot_cues,MVBU_Object-method}
\alias{plot_diagnostics,MVBU_Stanfit-method}
\title{Plot functions for MVBU objects}
\usage{
plot_categories(x, ...)

plot_categorization_function(x, ...)

plot_parameters(x, ...)

plot_parameter_correlations(x, ...)

plot_parameters_pairwise(x, ...)

plot_cues(x, ...)

plot_diagnostics(x, ...)

## S7 method for class <MVBU_CategoryRepresentation>
plot_categories(
  x,
  cues = NULL,
  categories = NULL,
  aes = NULL,
  levels = NULL,
  limits = NULL,
  n_exemplars = 100,
  resolution = 100,
  ...
)

## S7 method for class <MVBU_CategoryRepresentationTemplate>
plot_categories(
  x,
  cues = NULL,
  categories = NULL,
  aes = NULL,
  levels = NULL,
  limits = NULL,
  n_exemplars = 100,
  resolution = 100,
  ...
)

## S7 method for class <MVBU_CognitiveModel>
plot_categories(
  x,
  cues = NULL,
  categories = NULL,
  aes = NULL,
  levels = NULL,
  limits = NULL,
  n_exemplars = 100,
  resolution = 100,
  ...
)

## S7 method for class <MVBU_Stanfit>
plot_categories(
  x,
  cues = NULL,
  categories = NULL,
  groups = NULL,
  aes = NULL,
  levels = NULL,
  limits = NULL,
  ndraws = 100,
  n_exemplars = 100,
  resolution = 100,
  show_exposure_data = FALSE,
  show_test_data = FALSE,
  ...
)

## S7 method for class <MVBU_CognitiveModel>
plot_categorization_function(
  x,
  cues = NULL,
  categories = get_category_labels(x)[1L],
  aes = NULL,
  levels = NULL,
  limits = NULL,
  decision_rule = "proportional",
  resolution = 100,
  ...
)

## S7 method for class <MVBU_Stanfit>
plot_categorization_function(
  x,
  cues = NULL,
  categories = get_category_labels(x)[1L],
  groups = NULL,
  aes = NULL,
  levels = NULL,
  limits = NULL,
  ndraws = 100,
  resolution = 100,
  show_exposure_data = FALSE,
  show_test_data = FALSE,
  ...
)

## S7 method for class <MVBU_CognitiveModel>
plot_parameters(x, pars = NULL, combine_into_single_plot = TRUE, ...)

## S7 method for class <MVBU_Stanfit>
plot_parameters(
  x,
  pars = NULL,
  categories = NULL,
  groups = NULL,
  ndraws = 100,
  combine_into_single_plot = TRUE,
  index_panels = FALSE,
  ...
)

## S7 method for class <MVBU_Stanfit>
plot_parameter_correlations(
  x,
  pars = NULL,
  categories = NULL,
  groups = NULL,
  cues = NULL,
  ndraws = 100,
  ...
)

## S7 method for class <MVBU_Stanfit>
plot_parameters_pairwise(
  x,
  pars = NULL,
  categories = NULL,
  groups = NULL,
  cues = NULL,
  ndraws = 100,
  ...
)

## S7 method for class <MVBU_Object>
plot_cues(x, cues = NULL, categories = NULL, ...)

## S7 method for class <MVBU_Stanfit>
plot_diagnostics(x, ...)
}
\arguments{
\item{x}{An MVBU object (\code{\link{MVBU_CategoryRepresentation}},
\code{\link{MVBU_CategoryRepresentationTemplate}},
\code{\link{MVBU_CognitiveModel}}, or \code{\link{MVBU_Stanfit}}).}

\item{...}{Additional arguments passed to specific plotting methods.}

\item{cues}{Character vector of cue names to plot (1D or 2D).}

\item{categories}{Character vector of category names. For
\code{plot_categories}, defaults to \code{NULL} (all categories). For
\code{plot_categorization_function}, defaults to the first category
(\code{get_category_labels(x)[1L]}).}

\item{aes}{Plot aesthetic: \code{"contour"}, \code{"fill-gradient"},
\code{"fill-discrete"}, or combinations like
\code{c("fill-gradient", "contour")}. Defaults to \code{"contour"} for 2D
plots. \code{"fill"} defaults to \code{"fill-gradient"} in all cases.
\code{"fill-discrete"} is only available for parametric representations.}

\item{levels}{Numeric vector or list specifying density / probability
levels. For \code{plot_categories}, defaults to two-tailed central
probabilities corresponding to 1, 2, 3, 4 sigma
(\code{2 * stats::pnorm(1:4) - 1}). For
\code{plot_categorization_function}, defaults to
\code{c(0.01, 0.10, 0.25, 0.50, 0.75, 0.90, 0.99)}.}

\item{limits}{Optional named list or numeric vector specifying axis / cue
limits (e.g. \code{list(F1 = c(200, 800), F2 = c(800, 2500))}). Defaults to
\code{NULL} (computed from category statistics or exemplars).}

\item{n_exemplars}{Integer specifying max number of exemplars to randomly
sample and plot for exemplar representations. Defaults to 100 (\code{NULL}
plots all exemplars).}

\item{groups}{Character vector of group names to plot for
\code{\link{MVBU_Stanfit}} objects. For \code{plot_parameters_pairwise}
and \code{plot_parameter_correlations}, defaults to \code{"prior"}
(available groups include \code{"prior"} and all exposure groups).
For \code{plot_parameters}, defaults to all groups, including the prior.}

\item{ndraws}{Number of posterior draws to use when plotting from
\code{\link{MVBU_Stanfit}}. Defaults to 100 (\code{NULL} uses all draws).}

\item{pars}{Character vector of parameter names to filter in
\code{plot_parameters}, \code{plot_parameter_correlations}, and
\code{plot_parameters_pairwise}. Defaults to \code{NULL} (all
parameters).}

\item{combine_into_single_plot}{Logical; if \code{TRUE} (default), combines
multi-panel parameter subplots into a single plot via \pkg{patchwork}.}
}
\value{
A \code{ggplot} object (or list of \code{ggplot} objects if
\code{combine_into_single_plot = FALSE}). Because output is a standard
\code{ggplot} / \pkg{patchwork} object, it can be further customized with
additional layers, scales, themes, or facets (e.g.
\code{+ facet_wrap(~ Category)}).
}
\description{
Generic plotting functions for MVBU objects, including cognitive models,
category representations, templates, stanfits, and staninput objects.
}
\details{
In parameter plots, scale is displayed as standard deviation
\eqn{\tau = \text{SD} = \sqrt{\text{diag}(\boldsymbol{\Sigma})}} (or the
expected standard deviation
\eqn{\sqrt{\text{diag}(\mathbf{S}) / (\nu - D - 1)}} for NIW/NIX models),
while off-diagonal covariances are plotted as dimensionless correlation
coefficients \eqn{\rho \in [-1, 1]}.

\code{plot_parameter_correlations} displays a correlation heatmap of
Pearson correlation coefficients (\eqn{\rho}) computed across MCMC draws
for all parameters across categories and selected groups. Parameters for
distinct groups form separate rows and columns without averaging across
groups. Parameter names indicate category, cue, and group index in their
subscripts (with subscript \code{0} denoting the prior group). Gray bounding
boxes along the diagonal group parameters belonging to each category.

\code{plot_parameters_pairwise} displays a matrix of pairwise parameter
relationships across categories and groups. The lower triangle shows
sample scatterplots with LOESS trendlines; the diagonal displays marginal
posterior auto-densities scaled to panel height; and the upper triangle
displays 2D posterior density contour lines (\code{geom_density_2d} with
normalized density contours). Points and curves are colored by group.
}
\seealso{
\code{\link{categorize}}, \code{\link{posterior}},
\code{\link{get_parameters}}, \code{\link{get_draws}}
}
